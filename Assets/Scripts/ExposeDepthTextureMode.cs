/*
using UnityEngine;
using UnityEditor;


[CustomEditor(typeof(Camera))]
public class ExposeDepthTextureMode : Editor
{
    public override void OnInspectorGUI()
    {
        base.OnInspectorGUI();

        Camera camera = (Camera)target;

        camera.depthTextureMode = (DepthTextureMode)EditorGUILayout.EnumPopup("Depth Texture Mode", camera.depthTextureMode);
    }
}
*/
